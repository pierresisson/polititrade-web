import { AccessDenied } from "@/components/app/access-denied";

export const metadata = {
  title: "Access Denied | PolitiTrades",
};

export default function AdminDeniedPage() {
  return <AccessDenied />;
}
